services:
  - name: trashmap-backend
    type: web
    dockerfile: Dockerfile
    dockerContext: backend
    envVars:
      - key: CORS_ORIGINS
        value: "*"
    healthCheckPath: /health

databases:
  - name: trashmap-db
    plan: free
